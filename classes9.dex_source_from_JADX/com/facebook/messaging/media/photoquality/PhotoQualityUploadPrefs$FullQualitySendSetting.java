package com.facebook.messaging.media.photoquality;

/* compiled from: log_count */
public enum PhotoQualityUploadPrefs$FullQualitySendSetting {
    NEVER(0),
    WIFI_ONLY(1);
    
    public final int value;

    private PhotoQualityUploadPrefs$FullQualitySendSetting(int i) {
        this.value = i;
    }
}
