package com.facebook.video.channelfeed;

import android.content.Context;
import android.util.AttributeSet;
import com.facebook.widget.CustomFrameLayout;
import javax.annotation.Nullable;

/* compiled from: survey_details */
public class ChannelFeedSocialContextHeaderView extends CustomFrameLayout {
    public ChannelFeedSocialContextHeaderView(Context context) {
        this(context, null);
    }

    private ChannelFeedSocialContextHeaderView(Context context, @Nullable AttributeSet attributeSet) {
        this(context, attributeSet, 0);
    }

    private ChannelFeedSocialContextHeaderView(Context context, @Nullable AttributeSet attributeSet, int i) {
        super(context, attributeSet, i);
        setContentView(2130903546);
    }
}
