package com.facebook.orca.compose;

import com.facebook.inject.AbstractProvider;
import com.facebook.inject.IdBasedProvider;
import com.facebook.messaging.media.upload.config.MediaUploadConfiguration;

/* compiled from: openingSource */
public class MediaUploadConfigurationMethodAutoProvider extends AbstractProvider<MediaUploadConfiguration> {
    public Object get() {
        return ComposeModule.m5011a(IdBasedProvider.a(this, 4168));
    }
}
