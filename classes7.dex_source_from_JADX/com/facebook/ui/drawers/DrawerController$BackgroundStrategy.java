package com.facebook.ui.drawers;

/* compiled from: ensureSync full refresh delayed because it was already performed recently. */
public enum DrawerController$BackgroundStrategy {
    ENSURE_BACKGROUND,
    DONT_ENSURE_BACKGROUND
}
