package com.facebook.photos.upload.receiver;

import android.content.Context;
import android.content.Intent;
import com.facebook.content.ActionReceiver;
import com.facebook.content.BroadcastReceiverLike;
import com.facebook.loom.logger.LogEntry.EntryType;
import com.facebook.loom.logger.Logger;

/* compiled from: funnel_event_enabled */
class ConnectivityChangeReceiver$InternalReceiver implements ActionReceiver {
    public void onReceive(Context context, Intent intent, BroadcastReceiverLike broadcastReceiverLike) {
        Logger.a(2, EntryType.LIFECYCLE_BROADCAST_RECEIVER_END, 1760957476, Logger.a(2, EntryType.LIFECYCLE_BROADCAST_RECEIVER_START, -1207022459));
    }
}
