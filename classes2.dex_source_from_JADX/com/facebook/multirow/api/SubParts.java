package com.facebook.multirow.api;

import android.view.View;

/* compiled from: messages */
public interface SubParts<E extends AnyEnvironment> {
    <P> void mo2755a(int i, SinglePartDefinition<P, ?, ? super E, ? extends View> singlePartDefinition, P p);

    <P> void mo2756a(SinglePartDefinition<P, ?, ? super E, ? extends View> singlePartDefinition, P p);
}
