package com.facebook.analytics2.logger;

import android.os.HandlerThread;

/* compiled from: target_url */
public interface HandlerThreadFactory {
    HandlerThread mo735a(String str);

    HandlerThread mo736a(String str, int i);
}
