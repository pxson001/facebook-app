package com.facebook.feed.rows.core.parts;

import com.facebook.inject.Lazy;
import com.facebook.multirow.api.AnyEnvironment;
import com.facebook.multirow.api.MultiRowPartWithIsNeeded;

/* compiled from: next_alarm_formatted */
public class MultiRowPartWithIsNeededUtil {
    public static <T extends MultiRowPartWithIsNeeded<?, ? extends AnyEnvironment>> Lazy<T> m7430a(Lazy<? extends T> lazy) {
        return lazy;
    }
}
