package com.facebook.graphql.model;

/* compiled from: only supports local data fetch. */
public class NodeHelper {
    public static GraphQLImage m6530a(GraphQLNode graphQLNode) {
        return graphQLNode.cf().j().L();
    }

    public static boolean m6531b(GraphQLNode graphQLNode) {
        return (graphQLNode.cf() == null || graphQLNode.cf().j() == null || graphQLNode.cf().j().L() == null) ? false : true;
    }
}
