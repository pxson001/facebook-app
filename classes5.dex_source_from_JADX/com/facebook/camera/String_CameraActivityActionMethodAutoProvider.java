package com.facebook.camera;

import android.content.Context;
import com.facebook.inject.AbstractProvider;

/* compiled from: android.hardware.camera.front */
public class String_CameraActivityActionMethodAutoProvider extends AbstractProvider<String> {
    public Object get() {
        return CameraModule.m18806a((Context) getInstance(Context.class));
    }
}
