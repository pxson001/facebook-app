package com.facebook.content;

import android.app.Activity;
import android.content.Context;
import android.content.Intent;
import android.support.v4.app.Fragment;

/* compiled from: sensor_times_ms */
public interface ExternalIntentHandler {
    boolean mo579a(Intent intent, int i, Activity activity);

    boolean mo580a(Intent intent, int i, Fragment fragment);

    boolean mo581a(Intent intent, Context context);
}
